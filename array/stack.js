//push  return length
let colors = ["red", "blue"];
let count = colors.push("green");
console.log("colors length:", count, "colors array:", colors);

//pop
let item = colors.pop();
console.log(item, colors.length);
