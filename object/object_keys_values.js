const data = { a: 1 }
const keyArr = Object.keys(data)
const valueArr = Object.values(data)
console.log(keyArr, keyArr.length) // 0 empty object
console.log(valueArr, valueArr.length) // 0 empty object
